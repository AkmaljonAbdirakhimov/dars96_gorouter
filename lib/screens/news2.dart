import 'package:flutter/material.dart';

class News2 extends StatelessWidget {
  const News2({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("News2"),
      ),
      body: const Center(
        child: Text('News2'),
      ),
    );
  }
}
