import 'package:flutter/material.dart';

class News3 extends StatelessWidget {
  const News3({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("News3"),
      ),
      body: const Center(
        child: Text('News3'),
      ),
    );
  }
}
